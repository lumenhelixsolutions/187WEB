---
name: computational-lab
description: Template for building a reproducible computational lab notebook or experiment.
origin: portfolio
---

# Computational Lab: {{title}}

## YAML frontmatter

```yaml
title: "{{title}}"
domain: [mathematical | biomedical | software | public-data | mixed]
created: {{YYYY-MM-DD}}
author: "{{author}}"
status: [draft | in-progress | complete | archived]
reproducible: [unknown | yes | partial | no]
```

## Research question / hypothesis

> {{question}}

## Environment

- OS:
- Language:
- Dependency manager:
- Lockfile:
- Container / environment file:
- Hardware notes:

## Inputs

| File / Source | Description | URL / Path | Checksum | Date accessed |
|---------------|-------------|------------|----------|---------------|
| {{input}} | {{description}} | {{URL/path}} | {{hash}} | {{YYYY-MM-DD}} |

## Parameters

| Parameter | Value | Rationale |
|-----------|-------|-----------|
| {{param}} | {{value}} | {{rationale}} |

## Seeds

- Random seed(s):
- Stochastic components:

## Procedure

1.
2.
3.

## Outputs

| Output | Description | Location | Generated by |
|--------|-------------|----------|--------------|
| {{output}} | {{description}} | {{path}} | {{script}} |

## Validation

- [ ] Clean install succeeds from lockfile.
- [ ] Fresh run matches expected outputs.
- [ ] All figures are script-generated.
- [ ] All inputs documented with URLs and checksums.

## Reproducibility statement

> This lab was run on `{OS}` with `{language} {version}`. Dependencies are pinned in `{lockfile}`. Random seed `{seed}` was used. Raw data were retrieved from `{URL}` on `{date}` with checksum `{hash}`. All transformations are in `{script/notebook}` at commit `{commit}`. Outputs were validated by `{validation command}` and match the sample outputs in `{path}`.

## Evidence ladder

| Claim | Evidence rung | Supporting source | Caveats |
|-------|---------------|-------------------|---------|
| {{claim}} | {{0–5}} | {{source}} | {{caveats}} |

## Next actions

1.
2.
3.
